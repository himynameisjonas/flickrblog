<%= link_to("Föregående", show_path(@photo.previous), :class => "previous") if @photo.previous  %>

<%= image_tag @photo.large_url %>

<%= link_to("Nästa", show_path(@photo.next), :class => "next") if @photo.next %>